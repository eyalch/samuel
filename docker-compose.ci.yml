version: '3.6'

services:
  backend:
    image: registry.gitlab.com/eyal0803/samuel/backend
    build:
      context: ./backend

  nginx:
    image: registry.gitlab.com/eyal0803/samuel/nginx
    build:
      context: .
      dockerfile: ./nginx/Dockerfile
